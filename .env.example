# Copie para .env.local e preencha com os valores do seu projeto Supabase
# (Project Settings → API). VITE_SUPABASE_ANON_KEY é pública por design — vai
# para o bundle do navegador — mas NUNCA cole aqui a service_role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
